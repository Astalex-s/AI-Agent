# 🤖 Telegram AI-Агент

Умный Telegram-бот с интеграцией OpenAI GPT и множеством инструментов для выполнения различных задач.

## ✨ Возможности

- 🔍 **Поиск в интернете** - поиск информации через DuckDuckGo
- 🌤️ **Погода** - получение прогноза погоды для любого города
- 💰 **Криптовалюты** - отслеживание курсов криптовалют через CoinGecko API
- 💵 **Обычные валюты** - получение курсов валют (EUR/USD/RUB и др.) через ExchangeRate API
- 📁 **Работа с файлами** - чтение и запись файлов
- 💻 **Терминальные команды** - безопасное выполнение команд
- 📱 **QR-коды** - генерация QR-кодов с отправкой изображений в чат
- 🧠 **Контекстная память** - сохранение истории диалога

## 📋 Требования

- Python 3.10+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API Key (получить на [platform.openai.com](https://platform.openai.com))

## 🚀 Установка

1. **Клонируйте репозиторий или скачайте проект**

2. **Создайте виртуальное окружение** (рекомендуется):
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение**:
   - Windows:
     ```bash
     venv\Scripts\activate
     ```
   - Linux/Mac:
     ```bash
     source venv/bin/activate
     ```

4. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

5. **Создайте файл `.env` в корне проекта**:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
AI_MODEL=gpt-4
AI_TEMPERATURE=0.7
```

## 🎯 Запуск

```bash
python -m bot.main
```

Или:

```bash
python bot/main.py
```

После запуска бот будет готов к работе. Найдите его в Telegram и отправьте команду `/start`.

## 📖 Команды бота

- `/start` - Начать работу с ботом (показывает клавиатуру с кнопками)
- `/help` - Показать справку
- `/clear` - Очистить историю диалога
- `/status` - Показать статус бота и доступные инструменты

## ⌨️ Кнопки клавиатуры

Бот имеет удобную клавиатуру с кнопками для быстрого доступа к функциям:

- **🌤️ Погода** - запрашивает город и показывает погоду
- **💰 Криптовалюта** - запрашивает криптовалюту и показывает курс
- **💵 Валюта** - запрашивает пару валют и показывает курс
- **🔍 Поиск** - запрашивает поисковый запрос и ищет информацию
- **📱 QR-код** - запрашивает данные и генерирует QR-код
- **❓ Помощь** - показывает справку
- **📊 Статус** - показывает статус бота

**Важно**: При нажатии кнопки бот запрашивает дополнительную информацию. Вы также можете писать запросы напрямую в чат без использования кнопок.

## 💡 Примеры использования

### Погода
```
Какая погода в Москве?
```
Или через кнопку: нажать "🌤️ Погода" → ввести "Москва"

### Криптовалюты
```
Сколько стоит Bitcoin?
Курс Ethereum в USD
bitcoin, usd
```
Или через кнопку: нажать "💰 Криптовалюта" → ввести "bitcoin, usd"

### Обычные валюты
```
Какой курс USD/EUR?
Курс EUR к RUB
USD to RUB
EUR
```
Или через кнопку: нажать "💵 Валюта" → ввести "USD/EUR"

**Форматы ввода**:
- `USD/EUR` - курс доллара к евро
- `USD to EUR` - курс доллара к евро
- `EUR` - курс доллара к евро (USD как базовая валюта)

### Поиск
```
Найди информацию о Python
Что такое LangChain?
```
Или через кнопку: нажать "🔍 Поиск" → ввести "Python"

### QR-коды
```
Создай QR-код для https://example.com
Сгенерируй QR-код с текстом "Hello World"
```
Или через кнопку: нажать "📱 QR-код" → ввести URL или текст

QR-коды сохраняются в директории `temp_qr_codes/` и автоматически отправляются как изображения в чат, после чего файл удаляется.

### Файлы
```
Прочитай файл data.txt
Создай файл note.txt с текстом "Заметка"
```

### Терминальные команды
```
Выполни команду: ls -la
Покажи текущую директорию
```

## 📁 Структура проекта

```
AI-AGENT/
├── agent/                 # Модуль AI-агента
│   ├── agent.py          # Основная логика агента
│   ├── tools.py          # Инструменты агента
│   ├── logger_config.py  # Конфигурация логирования
│   └── memory.json       # Файл памяти агента
├── bot/                  # Модуль Telegram бота
│   ├── main.py          # Главный файл бота
│   ├── config.py        # Конфигурация бота
│   ├── handlers/        # Обработчики сообщений
│   │   ├── commands.py  # Обработчики команд
│   │   └── messages.py  # Обработчики сообщений и кнопок
│   ├── keyboards/       # Клавиатуры
│   │   └── inline.py    # Inline клавиатуры и кнопки
│   ├── messages/        # Тексты сообщений
│   │   └── texts.py     # Текстовые сообщения бота
│   └── utils/           # Утилиты
│       ├── qr_extractor.py      # Извлечение QR-кодов
│       ├── message_splitter.py  # Разбиение сообщений
│       └── user_states.py       # Управление состояниями пользователей
├── temp_qr_codes/        # Временная директория для QR-кодов (автосоздается)
├── logs/                # Логи приложения
├── .env                 # Переменные окружения (не в git)
├── requirements.txt     # Зависимости Python
└── README.md           # Документация
```

## 🔧 Настройка

### Модель OpenAI

По умолчанию используется `gpt-4`. Вы можете изменить модель в файле `.env`:

```env
AI_MODEL=gpt-3.5-turbo
```

Доступные модели:
- `gpt-4` - самая мощная модель (рекомендуется)
- `gpt-3.5-turbo` - более быстрая и дешевая модель

### Температура

Контролирует креативность ответов (0.0 - 1.0):

```env
AI_TEMPERATURE=0.7
```

## 🐛 Решение проблем

### Ошибка импорта LangChain

Если возникают ошибки импорта LangChain, обновите зависимости:

```bash
pip install --upgrade langchain langchain-openai langchain-community langchain-core
```

### Ошибка 403 от OpenAI API

Если вы получаете ошибку 403 "unsupported_country_region_territory", это означает, что ваш регион не поддерживается OpenAI. Решения:

1. Используйте VPN или прокси
2. Настройте прокси через переменные окружения:
   ```env
   HTTP_PROXY=http://proxy.example.com:8080
   HTTPS_PROXY=http://proxy.example.com:8080
   ```

### Бот не отвечает

1. Проверьте, что токен бота указан правильно в `.env`
2. Убедитесь, что бот запущен (`python -m bot.main`)
3. Проверьте логи в директории `logs/`

### QR-код не отправляется

1. Убедитесь, что установлены библиотеки: `qrcode[pil]` и `Pillow`
2. Проверьте права на запись в директорию проекта
3. Проверьте, что директория `temp_qr_codes/` создается автоматически
4. Проверьте логи для подробной информации об ошибках

### Курс валют не получается

1. Проверьте интернет-соединение
2. Убедитесь, что вы используете правильный формат ввода (например, `USD/EUR`)
3. Проверьте логи для подробной информации об ошибках

## 📝 Логирование

Все логи сохраняются в директории `logs/` с именем файла `agent_YYYYMMDD.log`. Логи включают:

- Все запросы пользователей
- Ответы агента
- Ошибки и исключения
- Работу инструментов
- Состояния пользователей при работе с кнопками

## 🔒 Безопасность

- **Терминальные команды**: Выполняются с ограничениями безопасности. Опасные команды блокируются.
- **API ключи**: Храните `.env` файл в безопасности и не коммитьте его в Git.
- **Файлы**: Бот работает с файлами в директории проекта. Будьте осторожны с командами удаления файлов.
- **QR-коды**: Временные файлы QR-кодов автоматически удаляются после отправки.

## 🎯 Особенности работы

### Работа с кнопками

При нажатии кнопки бот переходит в режим ожидания дополнительной информации:
1. Пользователь нажимает кнопку (например, "🌤️ Погода")
2. Бот запрашивает дополнительную информацию ("Для какого города показать погоду?")
3. Пользователь вводит данные ("Москва")
4. Бот обрабатывает запрос и отправляет результат

### Прямые запросы

Вы можете писать запросы напрямую в чат без использования кнопок. Бот автоматически определит, какой инструмент использовать.

### QR-коды

QR-коды генерируются в директории `temp_qr_codes/` с именем `qr_code.png` и автоматически отправляются как изображения в чат. После отправки файл удаляется для экономии места.

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте логи в `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность настроек в `.env`
4. Убедитесь, что у вас есть доступ к интернету для работы API

## 🎉 Благодарности

- [OpenAI](https://openai.com) - за GPT модели
- [LangChain](https://langchain.com) - за фреймворк для агентов
- [pyTelegramBotAPI](https://github.com/eternnoir/pyTelegramBotAPI) - за библиотеку Telegram бота
- [ExchangeRate API](https://www.exchangerate-api.com) - за бесплатный API курсов валют
- [CoinGecko](https://www.coingecko.com) - за API криптовалют
- [Open-Meteo](https://open-meteo.com) - за бесплатный API погоды
